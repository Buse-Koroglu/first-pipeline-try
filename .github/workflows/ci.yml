name: First Pipeline Try Main

on:
  push:

jobs:
  first-ci:
    runs-on:
      - ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Hello CI/CD
        run: echo "Hello I'm learning CI/CD"
      - name: Goodbye CI/CD
        run: echo "Bye Bye"
    

